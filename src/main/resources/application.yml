#spring.application.name=mysql_example

spring:
  application:
    name: mysql_example

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://mysql-container:3306/post_db?autoReconnect=true&useUnicode=true&serverTimezone=Asia/Seoul
    username: dh
    password: dh

  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: create
    show-sql: true

jwt:
  secret : ThisisTestThisisTestThisisTestThisisTestThisisTestThisisTest